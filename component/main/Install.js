var React = require( 'react' );
var Router = require('react-router');
var RouteHandler = Router.RouteHandler;

var Install = React.createClass({
	displayName: 'Install',
	render: function() {
		return <h3>Install</h3>;
	}
});

module.exports = Install;
